<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Kintsugi" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand colors. -->
        <item name="colorPrimary">@color/royal_purple</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="colorSecondary">@color/deep_violet</item>
        <item name="colorOnSecondary">@color/soft_lavender</item>
        <item name="colorAccent" tools:ignore="NewApi">@color/luxury_gold</item>

        <!-- Status bar and navigation bar -->
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
        <item name="android:windowBackground">@color/dark_background</item>

        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>